@model CeyhunApplication.Models.PopularTag

@{
    ViewData["Title"] = "Delete";
}
<div class="container-fluid">
    <h1>Delete</h1>

    <h3>Are you sure you want to delete this PopularTag?</h3>
    <div>
        <dl class="row">
            <dt class = "col-sm-2">
                @Html.DisplayNameFor(model => model.Title)
            </dt>
            <dd class = "col-sm-10">
                @Html.DisplayFor(model => model.Title)
            </dd>
        </dl>
    
        <form asp-action="Delete">
            <input type="hidden" asp-for="Id" />
            <button btn-action="delete"></button>
            <a asp-action="Index"><button btn-action="list"></button></a>
        </form>
    </div>
</div>